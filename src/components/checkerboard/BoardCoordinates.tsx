
import React from 'react';

const BoardCoordinates: React.FC = () => {
  // Empty component as we're now adding coordinates directly in BoardSquare
  return null;
};

export default BoardCoordinates;
